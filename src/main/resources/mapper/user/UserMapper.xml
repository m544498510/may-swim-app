<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.may.user.dao.IUserDAO" >
  <resultMap type="User" id="userResultMap">
    <id property="userId" column="user_id"/>
    <result property="userName" column="user_name"/>
    <result property="userEmail" column="user_email"/>
    <result property="userPsd" column="user_psd"/>
    <result property="userCreateTime" column="user_create_time"/>
  </resultMap>

  <!-- 根据id -->
  <select id="getUserById" parameterType="int"
          resultType="User" resultMap="userResultMap">
    <![CDATA[
            SELECT * from t_user
                WHERE user_id = #{userId}
        ]]>
  </select>

<!--
  &lt;!&ndash; 查询学生列表 &ndash;&gt;
  <insert id="insertUser" parameterType="com.may.user.model.User"
          resultType="int" resultMap="userResultMap">
    <![CDATA[
            SELECT * from STUDENT_TBL
        ]]>
  </insert>
-->
</mapper>